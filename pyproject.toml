[project]
name = "hermes-archive-scraper"
version = "0.1.0"
description = "eBay scraper for the Hermès Archive dataset"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27",
    "tqdm>=4.66",
]

[tool.uv]
package = false
